"""utils/jersey.py — jersey color (HSV) as a shared identity anchor.

NOT YET BUILT. Implemented in Step 4.

Dominant torso color in HSV, classified to a named color. Acts as a permanent
identity anchor and a hard team-side gate; works on a single stream.
"""
