"""utils/court_view.py — live top-down minimap.

NOT YET BUILT. Implemented in Step 5.

Render the full real padel-court template (sidelines, service lines, center
service line, net) and overlay player dots + ball from fused court coordinates.
"""
