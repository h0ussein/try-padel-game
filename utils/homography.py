"""utils/homography.py — calibration + pixel->court mapping.

NOT YET BUILT. Implemented in Step 5.

For EACH camera, findHomography from the SAME real court line intersections into
one shared 20 m x 10 m padel-court template. Both cameras must use the same
reference points/origin or fusion (Step 6) cannot match players.
"""
