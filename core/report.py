"""core/report.py — session logging + JSON/PDF reports.

NOT YET BUILT. Implemented in Step 10.

JSON (machine) + PDF (fpdf2, human) per session. Use a custom NumpyEncoder so the
JSON writer does not fail on numpy types.
"""
